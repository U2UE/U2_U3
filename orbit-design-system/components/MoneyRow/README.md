# MoneyRow

금액 변경·입금 한 줄(`.mr`).

- 묶음 Badge(`.gch`) · 내용(클릭 편집) · 날짜(mono 10px) · 금액(mono 12px 600, 음수 `bad`) · 시트 토글.
- 시트 토글 `.sy`: 점선 `line-2` 「시트」 → 옮겨 적으면 `.on` `accent-soft` 「시트 ✓」. 안 옮긴 변경 수는 보기 전환 칸에 amber 숫자로 뜬다.
- 입력: `.madd` = 묶음 셀렉트 + 「카드 뒷면 6장 추가 / 18000 · 줄면 -」.

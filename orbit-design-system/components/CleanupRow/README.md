# CleanupRow

메일 정리 후보 한 줄: 체크박스 · 제목/보낸 이 · 분류 배지.

- `.chdr` 머리줄(「전체 선택」 체크 + 개수), `.crow` 행: 20px 체크 칸(accent-color `accent`), 제목 13px, 보낸 이 12px `ink-3`.
- 배지 `.b`: 기본 중립, `.promo` 광고 = accent, `.ship` 30일 지난 배송 = warn.
- 보안·계정 알림은 후보에서 뺀다. 실행 버튼 문구는 「선택 항목 휴지통으로 이동」.

# FileRow

Drive 파일 한 줄: 종류 약어 아이콘 · 파일명 · 상대시간.

- `.ico` 34px `radius-5`, mono 10px 약어. 폴더 `.folder`(accent), AI·PSD `.ai`(file-ai).
- 이름 500 말줄임, 링크 hover에 밑줄. 시각은 오른쪽 mono 12px `ink-2`.
- 소유자·용량·절대시간은 행의 `title` 툴팁으로 보낸다(260924).

# MailRow

메일 한 통: 안 읽음 점 · 보낸 이 · 제목 · 상대시간.

- 8px 점 칸, 본문 칸, 시각 칸(mono 11px `ink-3`). 보낸 이 앞에 계정 Badge.
- 안 읽음 `.unread`: 점 `accent`, 보낸 이·제목 `ink`(제목 500). 읽음: 점 없음, `ink-2`.
- 행 전체가 클릭 대상(Gmail 열기). hover·focus에 `surface-2`.

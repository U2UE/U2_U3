# ProgressBar

얇은 진행바. 채움 색이 무엇의 진행인지 말한다.

- `.bar`: 5px, 트랙 `line`, `radius-3`, 채움 기본 `accent`(공부). 외주 안에서는 `gig`.
- `.pipe`: 이직 단계 칸 막대 4px — 지난 단계 `ink-3`, 남은 단계 `line`. 신호색 없음.
- 폭은 부모가 정한다(카드 안 flex:1, 외주 헤더 140px). 숫자 `n/N`을 옆에 mono로 둔다. 퍼센트는 표시하지 않는다.

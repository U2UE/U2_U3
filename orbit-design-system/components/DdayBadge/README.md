# DdayBadge

마감까지 남은 날을 말하는 알약 배지. 마감은 항상 red 계열이다.

- `.ddaybadge`(Schedule 헤더): `bad-soft` 바탕, mono 굵은 `D-1` + 일정 제목(말줄임, 최대 220px).
- `.gdue`(외주 헤더): 「9/25 · D-1」. 5일 이하 또는 지남이면 `.dead`(bad), 그 전엔 중립 `surface-2`/`ink-2`.
- amber를 쓰지 않는다(260924: amber 단계 삭제). 표기: `D-n`, `D-DAY`, 「지남」.

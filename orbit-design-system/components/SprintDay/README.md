# SprintDay

포트폴리오 스프린트의 하루 칸(4열 그리드, 860px 이하 2열).

- `.day`: `surface-2` 칸, 머리 11px 600 `ink-3` + 오른쪽 mono 카운트. 오늘 `.now`: `accent` 테두리·`surface` 면·`accent-ink` 머리. 지난 날 `.all`: `opacity-fin`.
- 체크 항목은 12px `ink-2`, 완료 `.ok`는 취소선 `ink-3`. 경고 한 줄은 11px `warn`.
